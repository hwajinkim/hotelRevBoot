package com.boot.sonorous.admin.repository;

import com.boot.sonorous.admin.entity.RoomImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SonorousRoomImageRepository extends JpaRepository<RoomImage, Integer> {
}
